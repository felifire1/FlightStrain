anthropic>=0.40.0
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
# Historical OpenSky access (xoolive/traffic + Trino driver). Heavy tree:
# pulls cartopy, shapely, pyproj, pandas, scipy, geopandas. ~150MB install.
# traffic-2.13 also installs pyopensky + trino-python-client transitively.
traffic>=2.13
certifi>=2024.0
# On Python 3.14/macOS, trino-python-client needs explicit CA bundle.
# Either set REQUESTS_CA_BUNDLE=$(python -c "import certifi;print(certifi.where())")
# or pass a configured requests.Session to trino.dbapi.connect (see scripts/historical_pull.py).
